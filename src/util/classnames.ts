import ClassNames from '../classnames';
import { classlist, classnames } from '@inlite/solidjs-typed-classlist';

const cn = classnames<ClassNames>;
const cl = classlist<ClassNames>;
export { cn, cl };
